import { CustomValidatorsFunction } from './custom-validators-function';

describe('CustomValidatorsFunction', () => {
  it('should create an instance', () => {
    expect(new CustomValidatorsFunction()).toBeTruthy();
  });
});
